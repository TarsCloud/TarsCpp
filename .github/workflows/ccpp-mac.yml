name: C/C++ CI

on: 
  push:
    branches:
    - "2.0"

jobs:
  build:

    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: install
      run: brew install -y bison flex
    - name: git
      run: git submodule update --init --recursive
    - name: configure
      run: mkdir build;cd build;cmake ..
    - name: make
      run: cd build; make
    - name: make run
      run: cd build; make run-all

